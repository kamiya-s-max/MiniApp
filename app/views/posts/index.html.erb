<div class="container">
  <div class="row">
    <div class="col l12 m6">
      <% @posts.each do |post|%>
        <%= render partial: "post", locals: { post: post } %>  
      <% end %>
        <%= paginate(@posts)%>
    </div>
  </div>
</div>